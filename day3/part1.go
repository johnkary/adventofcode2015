package main

import (
// "strings"
)

func part1(input string) int {
	return 0
	// Convert ^ > v < to X{-1}, X{1}, Y{1}, Y{-1}
	// Convert XY to v.Visit(Coord{x, y})
	// Derive visited houses
	// x := 0
	// for _, direction := range strings.(input, "") {
	//     area += getPaperArea(dimensions)
	// }

	// return area
}
